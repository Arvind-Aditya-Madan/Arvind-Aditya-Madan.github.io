To make the UNC Baseball Attendance report visuals render, copy the original README_ASSETS folder from the project repo into this reports folder, so the path becomes:

reports/README_ASSETS/

The converted report HTML already points to those image paths.
